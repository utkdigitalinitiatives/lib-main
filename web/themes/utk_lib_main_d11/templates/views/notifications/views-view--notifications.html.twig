{#
/**
 * @file
 * Renders the Notifications view without the default Views wrappers.
 *
 * Notifications sit directly below the site navigation, so extra view title,
 * header, and content wrappers are intentionally omitted.
 *
 * @see views-view.html.twig
 */
#}

{{ rows }}
