name: "Labeled Email Field"
description: "Provides a reusable multi-value field for storing labeled email addresses (label + email pairs)."
type: module
package: Custom
version: VERSION
core_version_requirement: "^11"
dependencies:
  - drupal:field
