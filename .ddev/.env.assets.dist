# Copy this file to .ddev/.env.assets and fill in real values.
# Do not commit .ddev/.env.assets.

AZURE_PUBLIC_BLOB_URL="https://{blog.storage.url}/{public-container}?{SAS_TOKEN}"

# FUTURE: If private:// is moved to Azure Blob, uncomment and set:
# AZURE_PRIVATE_BLOB_URL="https://{blog.storage.url}/{private-container}?{SAS_TOKEN}"
